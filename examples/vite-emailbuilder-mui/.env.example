# Image Upload Configuration (optional)
# If these are not set, the upload button is hidden and only the URL input is shown.

# The URL of your deployed Cloudflare Worker
VITE_UPLOAD_WORKER_URL=https://your-worker.your-subdomain.workers.dev

# The secret API key — must match the UPLOAD_API_KEY secret set on the Worker
# Generate a strong random string, e.g: openssl rand -hex 32
VITE_UPLOAD_API_KEY=your-secret-api-key
